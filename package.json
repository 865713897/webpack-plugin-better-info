{
  "private": true,
  "scripts": {
    "build": "pnpm -r --filter ./packages/** run build",
    "log": "changelogen --output"
  },
  "devDependencies": {
    "changelogen": "^0.5.5",
    "typescript": "^5.5.3",
    "unbuild": "^2.0.0"
  }
}
